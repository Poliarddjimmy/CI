<%= form_with(model: contact_file) do |form| %>
<% if contact_file.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(contact_file.errors.count, "error") %> prohibited this contact_file from being saved:</h2>

  <ul>
    <% contact_file.errors.each do |error| %>
    <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>


<div class="form-group">
  <%= form.label :"Select you file", class: "form-label mt-4" %>
  <%= form.file_field :file_url, class: "form-control" %>
</div>



<%= form.hidden_field :user_id, value: current_user.id %>

<div class="form-group mt-4">
  <%= form.submit :"Add Contact", class: "btn btn-info" %>
</div>
<% end %>